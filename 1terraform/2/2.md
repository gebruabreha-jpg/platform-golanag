For a production web app,you need:-
Networking — HTTPS, DNS, firewall.................N
Compute — run your Go binary (containerized)......C
storage(Database) — persistent storage............S
Secrets — DB credentials, API keys
AWS Minimum (ECS Fargate — Serverless Containers)
Azure Minimum (Container Apps — Serverless Containers)

ALB(N)+ Fargate(C) + RDS t3.micro(S).
Internet → ALB (HTTPS) → ECS Fargate (Go container) → RDS PostgreSQL
                    ↓
              CloudWatch (logs)